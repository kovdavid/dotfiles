[alias]
    st   = -c color.status=false status
    ci   = commit
    br   = branch
    co   = checkout
    me   = merge --no-ff
    ms   = merge --squash
    di   = diff
    din  = diff --name-status
    dic  = diff --cached
    lo   = log --oneline --reverse
    lg   = log --oneline --graph --all
    ld   = log --oneline --reverse HEAD ^develop --no-merges
    lm   = log --oneline --reverse HEAD ^master --no-merges
    re   = rebase -i
    mt   = mergetool
    root = !pwd
[user]
	name = Davs
	email = kovdavid@gmail.com
[core]
	editor = /usr/bin/vim
	pager = cat
	trustctime = false
[diff]
	external = git-meld
[merge]
    external = meld
[color]
	ui = auto
[grep]
	lineNumber = true
