#!/bin/bash

if [[ $(uname -a) == Darwin* ]] ; then
    CLIPBOARD="pbcopy"
else
    CLIPBOARD="xclip -in -filter -selection primary | xclip -in -selection clipboard"
fi

tmux capture-pane -J -p -e | grep -oE '(https?)://.*[^>]' | uniq | fzf-tmux -d20 --no-preview | $CLIPBOARD
