#!/bin/bash

if [ -z "$1" ] ; then
    echo "Usage: $0 file"
    exit
fi

BASENAME=${1%.*}
ffmpeg -i "$1" "$BASENAME.flac"
flac -c -d "$BASENAME.flac" | lame -V0 - "$BASENAME.mp3"
rm "$BASENAME.flac"
