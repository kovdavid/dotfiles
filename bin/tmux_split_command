#!/usr/bin/env bash

exec > >(tee -a /tmp/$(basename $0)) 2>&1

WINDOW_PATH=$(tmux display-message -p "#{@window_path}")
if [ -n "$WINDOW_PATH" ]; then
    echo "$WINDOW_PATH"
else
    SESSION_PATH=$(tmux display-message -p "#{session_path}")
    echo "$SESSION_PATH"
fi
